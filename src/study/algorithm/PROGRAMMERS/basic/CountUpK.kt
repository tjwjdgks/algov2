package study.algorithm.PROGRAMMERS.basic

class CountUpK {
    fun solution(start_num: Int, end_num: Int): IntArray {
        var answer: IntArray = (start_num..end_num).toList().toIntArray()
        return answer
    }
}